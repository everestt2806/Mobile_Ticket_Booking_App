<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Style for a rounded button -->
    <style name="RoundedPurpleButton" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/rounded_button</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">16sp</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:minHeight">48dp</item>
    </style>


    <!-- Style for circular shape -->
    <style name="CircularShape">
        <item name="shapeAppearanceOverlay">@style/CircularShapeAppearance</item>
    </style>

    <!-- Define a circular shape appearance -->
    <style name="CircularShapeAppearance">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
    <style name="RoundedImageView">
        <item name="cornerSize">8dp</item>
    </style>
</resources>

